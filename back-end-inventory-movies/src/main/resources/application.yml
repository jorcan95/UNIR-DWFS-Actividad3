## Configuracion de Spring
spring:
  application:
    name: ms-inventory-movies

## Configuracion de Instancia 
server:
  port: 8081

## Configuracion del cliente de Eureka
eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URL:http://localhost:8761/eureka}
  instance:
    preferIpAddress: true
    hostname: ms-inventory-movies

##Elasticsearch config
elasticsearch:
  host: ${ELASTICSEARCH_HOST:unir-search-4989434682.us-east-1.bonsaisearch.net}
  credentials:
    user: ${ELASTICSEARCH_USER:regfop5esb}
    password: ${ELASTICSEARCH_PWD:h9spulsagu}
logging:
  level:
    tracer: TRACE
